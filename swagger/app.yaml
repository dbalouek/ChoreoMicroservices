swagger: "2.0"

info:
  title: "My first API"
  version: "1.0"

paths:
  /items/:
    get:
      responses:
        '200':
          description: 'Fetch a list of zip codes'
          schema:
            type: array
            items:
              $ref: '#/definitions/Item'
    put:
      parameters:
        - name: item
          in: body
          schema:
            $ref: '#/definitions/Item'
      responses:
        200:
          description: Zip created
        201:
          description: Zip exists

definitions:
  Item:
    type: object
    required:
      - zip_code
    properties:
      zip_code:
        type: string